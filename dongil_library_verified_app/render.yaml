services:
  - type: web
    name: dongil-library-chatbot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python prepare_data.py
    startCommand: python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
